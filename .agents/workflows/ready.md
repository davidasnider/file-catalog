---
description: Add the ready-to-merge label to the current PR to satisfy the PR Gatekeeper checks
---

1. Execute the following command to add the `ready-to-merge` label to the current branch's Pull Request.
// turbo
```bash
gh pr edit --add-label "ready-to-merge"
```
