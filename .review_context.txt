PR #185 Review Context
=============================

Recent Commits:
---------------
1d1ae50 docs: update AGENTS.md with new memory guidelines
d7d908b docs: update AGENTS.md with new memory guidelines

PR Discussion History:
----------------------
### PR #185: docs: update documentation to reflect code modifications
**URL:** https://github.com/davidasnider/file-catalog/pull/185

**PR Description:**
> This PR updates AGENTS.md to include recently learned guidelines.
>
> ---
> *PR created automatically by Jules for task [5102602370987208688](https://jules.google.com/task/5102602370987208688) started by @davidasnider*

**Reviews:**
- **copilot-pull-request-reviewer** (COMMENTED): ## Pull request overview

This PR updates `AGENTS.md` with additional contributor/agent guidelines to reflect the current constraints and workflow expectations in the repository’s execution/review environment.

**Changes:**
- Added guidance on handling truncated command output during test/review runs.
- Documented git workflow caveats (shallow clones, avoiding direct push, update strategies).
- Added guidance on CLI flag verification and ruff-based formatting/linting practices.




<details>
<summary>Suppressed comments (1)</summary>

**AGENTS.md:118**
* These entries also exceed typical Markdown line-length limits, and this section immediately introduces a line-length guideline. Wrapping them onto indented continuation lines will keep the guidance consistent and easier to review.
```
- **Code Formatting**: Code formatting and linting should be performed using `uv run ruff format <modified_files>` and `uv run ruff check <modified_files>`. Avoid running formatting on the entire codebase (e.g., `uv run ruff format .`) to prevent out-of-scope changes that can introduce Python compatibility issues or clutter Pull Requests. When formatting Markdown files with ruff, it will fail unless preview mode is enabled; use `uv run ruff format --preview <modified_files>` when formatting Markdown.
- **Line Length**: When updating Markdown files, wrap text strings to adhere to standard line length limits (e.g., 80-120 characters) to pass project Markdown linting (like `ruff` or `markdownlint`) and prevent CI check failures.
- **Missing Module Testing**: The test suite in `tests/` uses `tests/conftest.py` which imports `sqlmodel`. If `sqlmodel` is missing from the environment, test collection will fail for the entire suite, even for unit tests that do not use database features.
```
</details>



---

💡 <a href="/davidasnider/file-catalog/new/main?filename=.github/skills/code-review/SKILL.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add a `code-review` agent skill</a> or configure MCP servers for context-aware, tailored reviews. <a href="https://docs.github.com/en/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review#mcp-servers-and-agent-skills" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn more in the docs.</a>

**Code Review Threads (Inline):**
- **copilot-pull-request-reviewer** on `AGENTS.md` (Unresolved):
> These newly added bullet items are each a single very long line, which hurts readability and can violate typical Markdown line-length linting. Please wrap each bullet’s text onto multiple indented continuation lines (while keeping the `-` list structure).
>
> This issue also appears on line 116 of the same file.
